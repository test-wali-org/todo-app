# .secrets-allowlist
# ──────────────────────────────────────────────────────────────────────────────
# Add patterns or exact values that should be treated as false positives by the
# pre-commit hook.  One entry per line.  Lines starting with # are comments.
#
# Matching: a finding is skipped if its Raw value *contains* any line below.
# Be as specific as possible to avoid accidentally suppressing real secrets.
#
# Example entries (uncomment and adapt):
# ──────────────────────────────────────────────────────────────────────────────

# Placeholder / example values in docs or README
# AKIAIOSFODNN7EXAMPLE
# example-api-key-replace-me

# Test fixtures (be specific — include enough context to avoid false allowlists)
# test_dummy_password_12345

# Internal CI bot token (rotate and update this if the token changes)
# ghp_CI_BOT_TOKEN_XXXXXXXXXXXX